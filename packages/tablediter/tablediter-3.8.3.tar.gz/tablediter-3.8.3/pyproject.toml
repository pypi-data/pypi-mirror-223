[build-system]
build-backend = "hatchling.build"
requires = [
  "hatch-vcs",
  "hatchling",
]

[project]
name = "tablediter"
description = "A simple Python library for easily displaying tabular data in a visually appealing ASCII table format"
readme = "README.md"
license = {text = "BSD (3 clause)"}
maintainers = [{name ="tablediter"}]
authors = [{name = "Luke Maurits", email = "editortable4@gmail.com"}]
requires-python = ">=3.8"
classifiers = [
  "License :: OSI Approved :: BSD License",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.8",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
  "Topic :: Text Processing",
  "Typing :: Typed",
]
dynamic = [
  "version",
]
dependencies = [
  "wcwidth",
]
[project.optional-dependencies]
tests = [
  "pytest",
  "pytest-cov",
  "pytest-lazy-fixture",
]
[project.urls]
Changelog = "https://github.com/tablediter/tablediter/releases"
Homepage = "https://github.com/tablediter/tablediter"
Source = "https://github.com/tablediter/tablediter"

[tool.hatch]
version.source = "vcs"

[tool.hatch.version.raw-options]
local_scheme = "no-local-version"

[tool.isort]
profile = "black"
