from setuptools import setup, find_packages

setup(
    name='subtraction-package',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List any dependencies here, if needed
    ],
)
