[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "azurly"
version = "0.1.1"
authors = [
  { name="Max Snoodijk"},
]
description = "A combined CDK and API package for Microsoft Azure"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["requests>=2.28.0"
]