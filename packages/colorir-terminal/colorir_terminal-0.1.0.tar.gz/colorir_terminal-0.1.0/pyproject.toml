[tool.poetry]
name = "colorir-terminal"
version = "0.1.0"
description = ""
authors = ["Jefferson L. Alionco <Jeffersonalionco@gmail.com>"]
readme = "README.md"
packages = [{include = "colorir_terminal"}]

[tool.poetry.dependencies]
python = "^3.11"
color-terminal = "^1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
