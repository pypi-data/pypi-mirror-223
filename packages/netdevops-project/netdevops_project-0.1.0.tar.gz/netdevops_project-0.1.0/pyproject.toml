[tool.poetry]
name = "netdevops-project"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "netdevops_project"}]

[tool.poetry.dependencies]
python = "^3.10"
ansible = "^8.2.0"


[tool.poetry.group.dev.dependencies]
requests = "2.30.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
