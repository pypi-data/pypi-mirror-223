[build-system]
requires = ["maturin>=0.15"]
build-backend = "maturin"

[project]
name = "rusty_chrono"
description = "Implementation of a datediff function for easier use with python"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
keywords = ["rust", "datediff", "date manipulation", "datetime", "date"]