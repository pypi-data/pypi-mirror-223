[tool.poetry]
name = "bsdm"
version = "0.1.0"
description = "Beautiful SVG Data Model"
authors = ["Loïc BAUD <loic.baud@tnpconsultants.com>"]
readme = "README.md"
packages = [{include = "bsdm"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
