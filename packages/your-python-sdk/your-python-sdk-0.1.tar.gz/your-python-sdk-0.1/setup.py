from setuptools import find_packages, setup

setup(
    name='your-python-sdk',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'requests',  # Add any dependencies here
    ],
)
