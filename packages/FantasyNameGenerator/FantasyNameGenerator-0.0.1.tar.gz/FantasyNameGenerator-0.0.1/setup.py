from setuptools import setup

setup(
    name='FantasyNameGenerator',
    version='0.0.1',
    install_requires=[],
    classifiers=[
        'Programming Language :: Python :: 3 :: Only'
    ],
)
